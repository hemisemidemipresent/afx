function main() {
    let results = document.getElementsByClassName('select-selected');

    artifactData.innerHTML = '';

    let monkeName = results[0].innerText;
    let durationStr = results[1].innerText;
}

function findArtifacts(ship, duration) {}
